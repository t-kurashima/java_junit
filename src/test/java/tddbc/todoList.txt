整数閉区間を示すクラス（あるいは構造体）をつくりたい。整数閉区間オブジェクトは下端点と上端点を持ち、文字列表現も返せる（例: 下端点 3, 上端点 8 の整数閉区間の文字列表記は "[3,8]"）。ただし、上端点より下端点が大きい閉区間を作ることはできない。整数の閉区間は指定した整数を含むかどうかを判定できる。また、別の閉区間と等価かどうかや、完全に含まれるかどうかも判定できる。

（例: 下端点 3, 上端点 8 の整数閉区間の文字列表記は "[3,8]"）

Todo
* [ ] 整数閉区間を示すクラス（あるいは構造体）を作る。
  * [x] 整数閉区間を示すクラスは下端点と上端点を文字列で取得できる(例："[3,8]")

  * [x] 整数閉区間を示すクラスは指定した整数を含むかどうかを判定できる。
  　  * [x] 指定した整数を含むことを判定できる。
  　        * [x] 下端の境界値を含むことを判定できる。
            * [x] 上端の境界値を含むことを判定できる。
            * [x] それ以外を含むことを判定できる。

  　　* [x] 指定した整数を含まないことを判定できる。
            * [x] 下端より下の値を含まないことを判定できる。
            * [x] 上端より上の値を含まないことを判定できる。

  * [x] 別の閉区間と等価かどうかが判定できる
    * [x] 別の閉区間と等価であることを判定できる
    * [x] 別の閉区間と等価でないことを判定できる

  * [ ] 別の閉区間と比べて完全に含まれるかどうかも判定できる。

準正常系
* [ ] 上端点より下端点が大きい閉区間の場合は、エラーを返す

 また、別の閉区間と等価かどうかや、完全に含まれるかどうかも判定できる。


していする？
文字列表現も返せる。
